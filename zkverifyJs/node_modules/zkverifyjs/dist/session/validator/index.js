"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateProofTypeOptions = validateProofTypeOptions;
const config_1 = require("../../config");
/**
 * Validates the library and curve type for a given proof type.
 * @throws {Error} - If the validation fails.
 * @param options
 */
function validateProofTypeOptions(options) {
    if (!options.proofType) {
        throw new Error('Proof type is required.');
    }
    const proofConfig = config_1.proofConfigurations[options.proofType];
    if (options.library && !proofConfig.requiresLibrary) {
        throw new Error(`Library cannot be set for proof type '${options.proofType}'.`);
    }
    if (!options.library && proofConfig.requiresLibrary) {
        throw new Error(`Library must be specified for proof type '${options.proofType}'.`);
    }
    if (options.curve && !proofConfig.requiresCurve) {
        throw new Error(`Curve type cannot be set for proof type '${options.proofType}'.`);
    }
    if (!options.curve && proofConfig.requiresCurve) {
        throw new Error(`Curve type must be specified for proof type '${options.proofType}'.`);
    }
}
//# sourceMappingURL=index.js.map